# TempleEx - Build artifacts & local files
# Dependencies
node_modules/

# Build outputs
# NOTE: TempleEx-full.lua IS committed on purpose — the bootloader downloads
# it via raw.githubusercontent.com, so the raw link must resolve.
# TempleEx-full.lua

# Workspace / runtime data
workspace/
workspace_test/
*.log
cache/
logs/

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets / Keys (never commit)
.env
*.key
*.pem

# Internal docs (not published)
TZ.md

# Test / Temp
*.tmp
*.temp
test_*